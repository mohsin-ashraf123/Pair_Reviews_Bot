# Fallback for Nixpacks / Railpack — monorepo with server + client
[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install", "npm run install:client"]

[phases.build]
cmds = ["npm run build:client"]

[start]
cmd = "npm start"
